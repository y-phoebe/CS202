// arrays

let students = ["Max", "Kyle", "Michael", "John", "Kayleen", "Chloe"];

console.log(students);

console.log(students[1])

//length

console.log(students.length);

// array methods

// pop() - will remove the last value on the list
// push() - adding a new character to array
// concat() - join/add new character
// indexOf() - gives the numeric placement on an item in an array

// let result = students.pop();
let result = concat("Ken");

console.log(students);
console.log(result);